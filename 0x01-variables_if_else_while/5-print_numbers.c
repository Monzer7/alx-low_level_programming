#include <stdio.h>
/**
 * main - entry point
 *
 * Discreption: all decimal ssystem digit
 * Return: Always 0 seccuess
 */
int main(void)
{
int m;
for (m = 0 ; m < 9 ; m++)
printf("%d", m);
putchar('\n');
return (0);
}
