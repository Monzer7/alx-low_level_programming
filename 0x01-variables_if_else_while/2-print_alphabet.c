#include <stdio.h>
/**
 * main - Entry point
 *
 * Discreption: a function to print the alphapete
 * Return: Always 0 (success)
 */
int main(void)
{
char m;
for (m = 'a'; m <= 'z'; m++)
putchar(m);
putchar('\n');
return (0);
}
