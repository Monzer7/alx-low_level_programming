#include <stdio.h>
/**
 * main - Entry point
 *
 * Discreption: this function show you neg and pos
 * Return: always 0 (success)
 */
int main(void)
{

int n;

if (n > 0)
{
printf("%d is positive, n\n");
}

else if (n == 0)
{
printf("%d is zero, n\n");
}

else(n < 0)
{
printf("%d is negative, n\n");
};

return (0);
}
