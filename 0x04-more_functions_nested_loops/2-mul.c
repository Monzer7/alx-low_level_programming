#include "main.h"
/**
 * mul - multiplt two numbers
 * @a: first number
 * @b: second number
 * @res: multiblication result
 * Return: multiplication result
 */
int mul(int a, int b)
{
int res;
res = a * b;
return (res);
}
